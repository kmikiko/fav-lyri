<div class="container mx-auto py-8">
  <h2 class="text-2xl mb-4"><%= @user.user_profile.name %>がフォローしている<br>ユーザー一覧</h2>

  <% if @following.any? %>
    <div class="grid grid-cols-1 gap-4">
      <% @following.each do |following| %>
        <div class="w-64 mx-auto bg-white shadow-md rounded-lg p-4">
          <%= link_to user_path(following.id) do %>
            <% if following.user_profile.icon && following.user_profile.icon.url %>
              <%= image_tag(following.user_profile.icon.url, class: "rounded-full w-12 h-12 mx-auto") %>
            <% else %>
              <%= image_tag("default.png", class: "rounded-full w-12 h-12 mx-auto") %>
            <% end %>
          <span class="block text-center mt-2"><%= following.user_profile.name %></span>
          <% end %>
        </div>
      <% end %>
    </div>
  <% else %>
    <p class="text-gray-500 mt-4 text-center">フォローしているユーザーはいません。</p>
  <% end %>
</div>