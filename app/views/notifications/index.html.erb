<% notifications = @notifications.reject { |notification| notification.visitor_id == current_user.id } %>
<% if notifications.any? %>
  <%= render partial: 'notifications/notification', collection: notifications %>
<% else %>
  <p>通知はありません</p>
<% end %>