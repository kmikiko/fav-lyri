<table>
  <tr>
    <th>歌詞</th>
    <th>詳細</th>
  </tr>

<% @lyrics.each do |lyric| %>
  <tr>
    <td><%= lyric.phrase %></td>
    <td><%= lyric.detail %></td>
    <td><%= link_to '詳細', lyric_path(lyric.id) %></td>
  </tr>
<% end %>
</table>
